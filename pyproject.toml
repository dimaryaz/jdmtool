[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "jdmtool"
version = "0.0.1"
dependencies = [
    'libusb1>=3.0.0',
    'tqdm>=4.1.0',
]

[project.scripts]
jdmtool = "jdmtool.main:main"
